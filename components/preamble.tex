\documentclass[10pt, a4paper]{article}
\usepackage{tikz, mhchem, incgraph, hyperref, romannum}
\igrset{border=4cm}
\usetikzlibrary{positioning, calc, decorations.text}
\tikzset{
    default/.append style={
            draw,
            rectangle,
            rounded corners,
            minimum width=2cm,
            minimum height=1cm,
            align=center
        },
    default_arrow/.append style={
            -latex,
            shorten >=5pt,
            shorten <=5pt
        },
}